# PoC signer-independent — dependências mínimas.
# Sem TensorFlow: o baseline é DTW (1-NN), não uma rede treinada (§5.2 do plano).
mediapipe==0.10.14
opencv-python>=4.9,<5   # 5.x move cv2.VideoWriter_fourcc, usado em record.py
numpy>=1.26,<2          # mediapipe 0.10.14 é compilado contra a ABI do numpy 1.x
dtaidistance>=2.3.10    # DTW multivariado com backend C (lib citada no §5.3)
fastdtw>=0.3.4          # fallback puro-Python quando o backend C não está disponível
scipy>=1.11             # métrica euclidiana usada pelo fallback
scikit-learn>=1.4       # matriz de confusão / métricas (§5.4)
matplotlib>=3.8         # plot da matriz de confusão
pyyaml>=6.0             # leitura do config.yaml

# Opcional (§5.5 — classificador treinado). FORA do escopo desta PoC:
# tensorflow>=2.14,<2.16
